import { StoredItem, StoredItemState } from '../../state/Item';

export const storedItemLoading: StoredItem = {
  id: '1',
  fieldsCentral: {},
  fieldsLocal: {},
  changes: [],
  state: StoredItemState.Loading
};
